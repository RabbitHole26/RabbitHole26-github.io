{
  "name": "stara torba",
  "version": "1.0.0",
  "description": "stronka dla mamy",
  "main": "main.js",
  "scripts": {
    "start": "vite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build --emptyOutDir",
    "publish": "gh-pages -d dist",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "author": "RabbitHole26",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "sass": "^1.69.5",
    "vite": "^5.0.10"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.2"
  }
}
